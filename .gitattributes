# Normalise text in the repo, but keep shell scripts LF or they will not run
# on the deploy runner.
* text=auto eol=lf
*.sh text eol=lf
scripts/hooks/* text eol=lf

# Binary assets: never line-ending translated.
*.png binary
*.gif binary
*.jpg binary
*.ico binary
*.webp binary
